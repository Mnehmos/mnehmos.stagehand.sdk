# Spec Kit Command Queue

Verified current workflow on 2026-09-13: constitution → specify → clarify → plan → checklist → tasks → analyze → implement → converge.

Initialize in the rebuild repository:

```bash
specify init --here --force --non-interactive --integration claude
```

Then process features in rebuild order. Re-ground every phase from the corpus, not from the previous command output.

## 01. FEAT-001 Mixed-Stream Parsing & Syntax
- `/speckit.specify` seeded from `specs/001-mixed-stream-parser/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 02. FEAT-002 Capability Registry, Validation & Introspection
- `/speckit.specify` seeded from `specs/002-capability-registry-validation/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 03. FEAT-003 Effect Compilation, Resolution & Safe Execution
- `/speckit.specify` seeded from `specs/003-effect-runtime/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 04. FEAT-005 Trace, Replay & Diagnostics
- `/speckit.specify` seeded from `specs/004-trace-replay/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 05. FEAT-006 Readiness & Synchronization
- `/speckit.specify` seeded from `specs/005-readiness-sync/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 06. FEAT-004 Compound Choreography & Beat IR
- `/speckit.specify` seeded from `specs/006-compound-choreography/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 07. FEAT-012 Shared Whiteboard Canvas
- `/speckit.specify` seeded from `specs/007-whiteboard/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 08. FEAT-007 Geospatial Camera & View Framing
- `/speckit.specify` seeded from `specs/008-geo-camera/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 09. FEAT-008 Geospatial Highlighting & Annotation
- `/speckit.specify` seeded from `specs/009-geo-annotation/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 10. FEAT-009 Geospatial Layers, Flows & Temporal Overlays
- `/speckit.specify` seeded from `specs/010-geo-layers-overlays/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 11. FEAT-010 World Pieces & Controlled Registry Proposals
- `/speckit.specify` seeded from `specs/011-geo-pieces-registry/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 12. FEAT-011 Evidence, Sources & Scene Presentation
- `/speckit.specify` seeded from `specs/012-evidence-presentation/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 13. FEAT-013 Classroom Avatar, Room & Camera Staging
- `/speckit.specify` seeded from `specs/013-classroom-staging/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 14. FEAT-014 Classroom Projector & Media Orchestration
- `/speckit.specify` seeded from `specs/014-classroom-media/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 15. FEAT-015 Lesson Interaction & Pedagogical State
- `/speckit.specify` seeded from `specs/015-lesson-interaction/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 16. FEAT-016 Chess Annotation Compatibility
- `/speckit.specify` seeded from `specs/016-chess-compatibility/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 17. FEAT-017 Word-Anchored DOM Presenter Choreography
- `/speckit.specify` seeded from `specs/017-dom-presenter/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.

## 18. FEAT-018 Host Model & Narration Configuration
- `/speckit.specify` seeded from `specs/018-host-model-narration-config/spec.md`.
- `/speckit.clarify` seeded only from its Open Questions/U-### references; do not invent ambiguity.
- `/speckit.plan` seeded from `plan.md`, `data-model.md`, and `contracts/`.
- `/speckit.checklist` seeded from the corresponding TEST rows in `32_PARITY_SUITE.md`.
- `/speckit.tasks`; reconcile generated tasks with the stable task ID shown in `00_TRACEABILITY.md`.
- `/speckit.analyze`; fix corpus inconsistencies before implementation.
- `/speckit.implement`.
- `/speckit.converge`; append remaining work until converged or an accepted DIV explains the difference.
